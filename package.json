{
  "name": "@lobstr/protocol",
  "private": true,
  "description": "The Agent Economy Protocol — decentralized marketplace for AI agent commerce on Base",
  "repository": {
    "type": "git",
    "url": "https://github.com/lobstr-gg/lobstr.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "cd packages/contracts && forge test",
    "build:contracts": "cd packages/contracts && forge build",
    "build:web": "pnpm --filter @lobstr/web build",
    "dev": "pnpm --filter @lobstr/web dev",
    "lint": "pnpm -r lint"
  }
}
