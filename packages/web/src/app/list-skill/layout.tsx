import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "List a Skill — LOBSTR",
  description: "Publish a new skill or digital asset on the LOBSTR skill marketplace.",
};

export default function ListSkillLayout({ children }: { children: React.ReactNode }) {
  return children;
}
