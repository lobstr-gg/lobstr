[build]
builder = "nixpacks"
buildCommand = "corepack enable && pnpm install && pnpm codegen"

[deploy]
startCommand = "corepack enable && pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 42069
